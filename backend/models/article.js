export default class Article {
  constructor(id, title, description, authorid, category, share) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.authorid = authorid;
    this.category = category;
    this.share = share;
  }
}
